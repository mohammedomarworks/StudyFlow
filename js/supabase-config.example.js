/* ==========================================================================
   supabase-config.example.js — Example Supabase Configuration Template
   --------------------------------------------------------------------------
   Copy this file to `js/supabase-config.local.js` or fill in `js/supabase-config.js`
   with your own Supabase project details.

   INSTRUCTIONS:
   1. Go to your Supabase project dashboard: https://app.supabase.com
   2. Navigate to Project Settings → API
   3. Copy your "Project URL" and paste it into `url` below.
   4. Copy your "Project API Key" (labeled `anon` / `public` or publishable `sb_pub_...`)
      and paste it into `publishableKey` below.
   5. NEVER use the `service_role` key in frontend code!
   ========================================================================== */

window.STUDYFLOW_SUPABASE_CONFIG = {
  url: "https://your-project-ref.supabase.co",
  publishableKey: "your-publishable-or-anon-key-here"
};
